#
# This file is GENERATED. Editing will not have permanent effects
#

#cmake_minimum_required(VERSION 3.9 FATAL_ERROR)

#if (JFC_BUILD_LIB)
    project("@NAME_value@"
        VERSION "@VERSION_value@" 
        DESCRIPTION "@DESCRIPTION_value@")

    add_library(${PROJECT_NAME} STATIC
        @SOURCE_LIST_value@
    )

    target_include_directories(${PROJECT_NAME} PRIVATE
        @INCLUDE_DIRECTORIES_value@
    )

    set_target_properties(${PROJECT_NAME} PROPERTIES RULE_LAUNCH_COMPILE "${CMAKE_COMMAND} -E time")

    set_property(TARGET ${PROJECT_NAME} PROPERTY C_STANDARD @C_STANDARD_value@)
    set_property(TARGET ${PROJECT_NAME} PROPERTY CXX_STANDARD @C++_STANDARD_value@)

    # This is hacky. This lets me treat subprojects that are part of an uber build like installed projects
    set(${PROJECT_NAME}_INCLUDE_DIR
        ${CMAKE_CURRENT_LIST_DIR}/include
        CACHE PATH "${PROJECT_NAME}_INCLUDE_DIR include directory" FORCE)
    
    set(${PROJECT_NAME}_LIBRARIES
        @LIBRARIES_value@
        CACHE PATH "${PROJECT_NAME}_LIBRARIES library object list" FORCE)
#endif()

#
# This file is GENERATED. Editing will not have permanent effects
#
